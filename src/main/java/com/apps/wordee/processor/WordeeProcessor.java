package com.apps.wordee.processor;

public class WordeeProcessor {
//    process method to create string of file contents, read each line, include regex to remove not allowed characters, add all words to an arraylist (split by spaces) in return response with each value
//    response builder which builds string
//    methods for complex parts

}
